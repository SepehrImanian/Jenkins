apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jenkins-slave01
  namespace: jenkins
spec:
  # storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi